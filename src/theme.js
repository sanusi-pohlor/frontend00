import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: [
      "Th Sarabun New",
      "sans-serif",
    ].join(","),
  },
  Form: {
    fontFamily: [
      "Th Sarabun New",
      "sans-serif",
    ].join(","),
  },
  barchart: {
    fontFamily: [
      "Th Sarabun New",
      "sans-serif",
    ].join(","),
  },
});

export default theme;
